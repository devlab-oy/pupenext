<% if @commodity.procurement_rows.present? %>
<br/>
<font class="head"><%= t('Liitetyt hankintakustannukset') %></font>
<br/><br/>
<table>
<tr>
  <th><%= t('Tunnus') %></th>
  <th><%= t('Summa') %></th>
  <th><%= t('Pvm') %></th>
</tr>
  <% @commodity.procurement_rows.each do |procrow| %>
  <tr>
    <td><%= procrow.tunnus  %></td>
    <td><%= procrow.summa %></td>
    <td><%= procrow.tapvm %></td>
  </tr>
  <% end %>
</table>
<% end %>
