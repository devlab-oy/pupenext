<%= return_link t('administration.transports.index.header'), transports_path %>

<br/>

<%= render 'administration/header', text: t('administration.transports.edit.header') %>
<%= render 'form' %>

<% if @transport.persisted? %>
  <br/>

  <%= button_to(t('.button_remove'),
                transport_path(@transport),
                data: { confirm: t('administration.transports.edit.confirm_remove') },
                method: :delete) %>
<% end %>
