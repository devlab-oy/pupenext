<%= return_link("Tilikartan rakenne", sum_levels_path) %>

<br/>

<%= render "administration/header", title: "Muokkaa tasoa" %>
<%= render 'form' %>

<% if @sum_level.persisted? %>
  <br/>

  <%= button_to(t('Poista taso'),
                sum_level_path(@sum_level),
                data: { confirm: t('Haluatko todella poistaa tämän tietueen?') },
                method: "delete") %>
<% end %>
