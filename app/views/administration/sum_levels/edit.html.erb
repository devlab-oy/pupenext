<%= return_link(SumLevel.model_name.human(count: 2), sum_levels_path) %>

<br/>

<%= render "administration/header", text: t('.header') %>
<%= render 'form' %>

<% if @sum_level.persisted? %>
  <br/>

  <%= button_to(t('.button_remove'),
                sum_level_path(@sum_level),
                data: { confirm: t('.confirm_remove') },
                method: "delete") %>
<% end %>
