<%= link_to "« #{t("Palaa ohjelmaan")}: #{t("Pakkaamo")}", packing_areas_path %>

<br/>

<%= render "administration/header", title: "Muokkaa pakkaamoa" %>

<%= render 'form' %>

<br />

<%= button_to t("Poista pakkaamo"), packing_area_path(@packing_area.id), confirm: t("Haluatko todella poistaa tämän tietueen?"), method: :delete if update_access? %>


