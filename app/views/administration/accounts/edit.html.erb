<%= link_to '« Palaa ohjelmaan: tilikartta', accounts_path %>
<br/>
<br/>
<font class="head">Muokkaa tiliä</font>
<%= render 'form',
           account: @account,
           levels: @levels,
           qualifiers: @qualifiers,
           oletus_alv_options: @oletus_alv_options %>
<br/>
<%= button_to('Poista tili',
              account_path(@account.id),
              confirm: 'Haluatko todella poistaa tämän tietueen?',
              method: "delete") %>
