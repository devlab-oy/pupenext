<%= return_link(Account.model_name.human(count: 2), accounts_path) %>

<br/>

<%= render "administration/header", text: t('.header') %>
<%= render 'form', account: @account,
                   levels: @levels,
                   qualifiers: @qualifiers,
                   oletus_alv_options: @oletus_alv_options %>

<% if @account.persisted? && update_access? %>
  <br/>

  <%= button_to(t('.button_remove'),
                account_path(@account.id),
                data: { confirm: t('.confirm_remove') },
                method: "delete") %>
<% end %>
