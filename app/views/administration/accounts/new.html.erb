<%= return_link("Tilikartta", accounts_path) %>

<br/>

<%= render "administration/header", text: t('.header') %>
<%= render 'form',
           account: @account,
           levels: @levels,
           qualifiers: @qualifiers,
           oletus_alv_options: @oletus_alv_options %>
