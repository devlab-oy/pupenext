<%
   # Locals
   #
   # f
   # resource
   # resource_name
%>


<div class="actions">
  <% text = resource.persisted? ? t('Päivitä') : t('Lisää') %>
  <% text = "#{text} #{resource_name}" %>
  <%= f.submit t(text), disable_with: t('Tallentaa') + '...' %>
</div>
