<% if @product.errors.any? %>
  message = "<font class='error'><%= @product.errors.full_messages.join('<br>') %></font>"
<% else %>
  message = "<font class='ok'><%= t('.update_success') %></font>"
<% end %>

$('<%= "#pending_updates_#{@product.id}" %> > .notifications').html(message + "<br><br>")
